# Copy Evidence Strip Acceptance

Before claiming this recipe was applied:

- [ ] The generated page includes a visible `Copy evidence strip` section.
- [ ] Each claim is paired with a real configured proof artifact or safe default repo artifact.
- [ ] The strip does not contain fake metrics, customer logos, customer endorsements, or unsupported guarantees.
- [ ] The strip works in both desktop and mobile visual QA.
- [ ] The primary CTA remains visible and unchanged unless the request explicitly changed it.
- [ ] `npm run qa:full` passes.
- [ ] If auto-selected, generated `README.md`, `AGENTS.md`, and `.agent/site.contract.yaml` record `copy-evidence-strip`.
