# Chief-of-staff briefing acceptance

Before claiming this recipe is applied:

- [ ] `src/data/briefing.json` is generated by `npm run build:briefing`.
- [ ] `npm run check:briefing` passes.
- [ ] The panel renders `#briefing` with exactly three top actions.
- [ ] Risk flags and verified wins are visible.
- [ ] The recommended recipe includes verification commands.
- [ ] Source counts are visible and link to static page sections.
- [ ] Copy says the briefing is static/public-safe, not live monitoring.
- [ ] No private tickets, raw chats, credentials, analytics, auth, telemetry, DB, or unsupported guarantees are introduced.
- [ ] `npm run qa:full` passes.
