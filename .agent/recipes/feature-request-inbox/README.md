# Feature Request Inbox Recipe

`feature-request-inbox` creates a static request intake queue for AgentSite improvements.

It creates:

- `src/data/requests.json`
- `src/components/FeatureRequestInbox.astro`
- `scripts/check-requests.mjs`

The inbox is a curated public backlog, not private ticket sync or live support intake.
